"""Папки приложения.

Считаем всё от места, где лежит сам скрипт, а не от текущей директории:
при запуске через ярлык рабочая папка может оказаться любой.
"""

from pathlib import Path

BASE = Path(__file__).resolve().parent.parent

# Готовые шортсы — рядом с приложением, чтобы не искать.
OUT = BASE / "shorts"

# Скачанное сырьё: видео, субтитры, список кандидатов.
WORK = BASE / "work"
